<h1>{$title}</h1>

<ul class="user-gallery">
{foreach $users as $user}
	<li>
	<a href="{link User:profile $user['id']}" title="{$user['fullname']} na TrekMap">
		<img width="80" height="80" src="{$presenter->linkGravatar(80, $user['email'], $user['female'])}" alt="gravatar &mdash; {$user['fullname']}">
		<strong>{$user['fullname']}</strong>
	</a>
	</li>
{/foreach}
</ul>

<p class="cleaner">
{if empty($identity)}
<a href="{link Account:login}">Staň se <strong>{=count($users) + 1}. borcem</strong>!</a> Nemusíš se ani registrovat.
{else}
Počet všech borců: <strong>{=count($users)}</strong>.
{/if}
</p>
